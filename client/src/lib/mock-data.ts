import type { Trip, Country, NewsAlert } from "@shared/schema";

// This file is kept for reference but we're using real API data
export const mockTrips: Trip[] = [];
export const mockCountries: Country[] = [];
export const mockAlerts: NewsAlert[] = [];
